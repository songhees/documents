spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: qwer1234
    url: jdbc:postgresql://localhost:5433/springJpa?currentSchema=public
    hikari:
      connection-test-query: SELECT 1
      idle-timeout: 30000
      connection-timeout: 30000
      schema: public
      maximum-pool-size: 5
  batch:
    jdbc:
      initialize-schema: always

batch:
  datasource:    
    driver-class-name: org.postgresql.Driver    
    url: jdbc:postgresql://localhost:5432/springJpa?currentSchema=batchdb
    username: postgres
    password: qwer1234

file:
  local: C:\Users\osh48\Downloads\